@model AdminConsole.Models.ContactsModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ContactsModel</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Vieta)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Vieta)
            @Html.ValidationMessageFor(model => model.Vieta)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Adrese)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Adrese)
            @Html.ValidationMessageFor(model => model.Adrese)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ExtraInfo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ExtraInfo)
            @Html.ValidationMessageFor(model => model.ExtraInfo)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
